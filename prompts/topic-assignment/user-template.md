# Topic-assignment user message template

A registering agent's mission:
{mission}

The current topic tree — each topic shows its path, name, and parent, and one is
the coordinator's seeded catch-all:
{existing_topics}

{retry_instruction}

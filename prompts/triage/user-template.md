# Triage user message template

The incoming message addressed to the orchestrator:
{incoming}

Sent by:
{sender}

The topic directory — each topic shows its path and the agents on it, and one is
the coordinator's seeded catch-all:
{topic_directory}

The agent directory — each agent shows its id, mission, and topics; route only to
these:
{agent_directory}

{retry_instruction}
